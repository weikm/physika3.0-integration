#include "collision/internal/collision_vec3.hpp"

namespace Physika {
class ManifoldUtil
{
public:
    // static void addContactPoint(crigid* r0, cplane* pln, crigid* r, vec3f& ptW, vec3f& nrmW, float depth) {}
};
};  // namespace Physika